Holding: nothing

Last job: added the step by step backup setup runbook to DURABILITY.md, so
the walkthrough survives a deleted chat. Nothing left for an agent on the
backup: creating the private repo, the token and the service role key are
all River, and no agent should hold a service role key.
Since:   2026-08-23
