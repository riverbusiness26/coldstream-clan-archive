Holding: HANDOFF.md, COORDINATOR.md
Doing:   coordinator session, correcting the backup state. The one green
         backup run was a manual dispatch at a pre v5 commit, not the
         nightly cron, and both documents currently read as if the nightly
         works.
Since:   2026-08-24
